{
  "$schema": "https://schema.railpack.com",
  "root": ".",
  "setup": ["[email protected]"],
  "install": ["pip install -r requirements.txt"],
  "deploy": ["gunicorn backend_MySQL:app --bind 0.0.0.0:$PORT"]
}
